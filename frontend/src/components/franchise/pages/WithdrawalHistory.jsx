import React from "react";
import { Box, Container, Typography } from "@mui/material";

export default function WithdrawalHistory() {
  return (
    <Box sx={{ minHeight: "100vh" }}>
      <Container>
        <Typography variant="h5">Withdrawal History Page</Typography>
      </Container>
    </Box>
  );
}